[
  {
    "id": "nhjsd7t2",
    "name": "Base Box",
    "description": "Creates a foundational parametrized box.",
    "parameters": {
      "length": 15,
      "width": 15,
      "height": 5
    },
    "code": "def make_base(self):\n    shape = cq.Workplane('XY').box(self.length, self.width, self.height)\n    self.model = shape",
    "isModified": false,
    "isExecuted": true
  },
  {
    "id": "step_add_bosses",
    "name": "Corner Bosses",
    "description": "Adds 4 cylindrical bosses to the corners of the plate.",
    "parameters": {
      "boss_radius": 1.5,
      "boss_height": 3,
      "boss_inset": 1.5
    },
    "code": "def add_bosses(self):\n    self.model = self.model.faces(\">Z\").workplane().rect(self.length - 2 * self.boss_inset, self.width - 2 * self.boss_inset, forConstruction=True).vertices().circle(self.boss_radius).extrude(self.boss_height)",
    "isModified": false,
    "isExecuted": true
  }
]